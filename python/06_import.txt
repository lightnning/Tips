# 同じ階層にあるファイルの import
ディレクトリ構造
.
├── foo.py
└── main.py # <= 呼び出し側
[main.py]
import foo

# 同じ階層にあるディレクトリの中にあるファイルの import
ディレクトリ構造

.
├── aaa/
│   ├── bbb/
│   │   └── bar2.py
│   └── bar.py
└── main.py # <= 呼び出し側
[main.py]
from aaa import bar
from aaa.bbb import bar2
